#include "CandyLib.h"
#include <stdio.h>

void Bmp2GameState(Pixel** &bitmap, int ScreenX, int ScreenY) {
    /*for(int i = 0; i < ScreenY; i++) {
        for(int j = 0; j < ScreenX; j++) {

            printf("(%d,%d,%d) ", bitmap[i][j].b, bitmap[i][j].g, bitmap[i][j].r);
        }
        printf("\n");
    }*/
    printf("ScreenX = %d ScreenY = %d\n", ScreenX, ScreenY);
}
